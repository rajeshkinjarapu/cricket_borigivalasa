# Git & Background Execution Preferences

- Do not automatically execute `git` commands (e.g. git add, git commit, git push) in the background.
- If the user asks to "send to git" or push changes, provide the exact shell commands for the user to copy and run themselves.
- Never run anything in the background without explicit permission, especially git operations.
